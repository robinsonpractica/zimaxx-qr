import{defineMiddleware}from"astro:middleware";export const onRequest=defineMiddleware(async(context,next)=>{context.locals.requestId=context.request.headers.get("cf-ray")??crypto.randomUUID();const started=Date.now();try{const response=await next();response.headers.set("x-request-id",context.locals.requestId);response.headers.set("x-frame-options","DENY");response.headers.set("x-content-type-options","nosniff");response.headers.set("referrer-policy","strict-origin-when-cross-origin");console.log(JSON.stringify({level:"info",event:"request",requestId:context.locals.requestId,path:context.url.pathname,status:response.status,durationMs:Date.now()-started}));return response}catch(error){console.error(JSON.stringify({level:"error",event:"request_failed",requestId:context.locals.requestId,path:context.url.pathname,message:error instanceof Error?error.message:"unknown"}));throw error}});
